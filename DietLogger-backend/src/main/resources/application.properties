spring.application.name=diet-logger
spring.datasource.url=jdbc:mysql://localhost:3306/dietlogger
spring.datasource.username=root
spring.datasource.password=@Mc7724033550

# Auto-create/update tables from the @Entity classes (fine for local dev)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- JWT ---
# Must be at least 32 characters for HS256. Change this to your own random string.
jwt.secret=change-this-to-a-long-random-secret-string-at-least-32-chars
jwt.expiration-ms=86400000

# --- CORS ---
cors.allowed-origin=http://localhost:5173

server.port=8080